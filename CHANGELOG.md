# 📓 Журнал изменений

Здесь Claude записывает, что менялось в проекте при каждом сохранении на GitHub
(командой `/app-push`). Новые записи добавляются **сверху** — так всегда виден свежий контекст
последних изменений.

Формат записи: дата, короткий заголовок и список того, что поменялось простыми словами.

---

## 2026-06-19 — Журнал создан

- Заведён журнал изменений. Дальше записи будут добавляться автоматически при каждом `/app-push`.
